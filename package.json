{
  "name": "carbon-footprint-platform",
  "version": "1.0.0",
  "description": "Zero-config Carbon Footprint Tracker (React + Node.js Monolith)",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
